<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".UserDashboardActivity" />
        <activity android:name=".RegisterActivity" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ClientConsultationActivity" />
        <activity android:name=".ITAssessmentActivity" />
        <activity android:name=".SolutionProposalActivity" />
        <activity android:name=".ImplementationActivity" />
        <activity android:name=".RequestServiceActivity" />
        <activity android:name=".MyDashboardActivity" />
        <activity android:name=".ManageUsersActivity" />
        <activity android:name=".ReviewRequestsActivity" />
        <activity android:name=".ScheduleMeetingsActivity" />
        <activity android:name=".DocumentsActivity" />
        <activity android:name=".ReportsActivity" />
        <activity android:name=".SettingsActivity" />

    </application>
</manifest>
